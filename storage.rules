rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /comprovantes/{advanceId}/{fileName} {
      allow read, write: if request.auth != null;
    }

    match /audios/{advanceId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
